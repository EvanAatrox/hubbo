import type { KeyInfo } from '@/utils/type.ts'
import { AESObject, RSAObject } from '@/utils/type.ts'
import { defineStore } from 'pinia'

/* 加密信息相关的store */
export const useEncryptInfoStore = defineStore('encryptStore', () => {

})
